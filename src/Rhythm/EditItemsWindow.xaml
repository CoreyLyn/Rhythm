<Window x:Class="Rhythm.EditItemsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rhythm — 编辑事项"
        Width="400"
        Height="500"
        ResizeMode="CanResize"
        Background="#1C1C1E"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="True">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2C2C2E"/>
            <Setter Property="Foreground" Value="#F2F2F7"/>
            <Setter Property="BorderBrush" Value="#3A3A3C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2C2C2E"/>
            <Setter Property="Foreground" Value="#F2F2F7"/>
            <Setter Property="CaretBrush" Value="#F2F2F7"/>
            <Setter Property="BorderBrush" Value="#3A3A3C"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="#F2F2F7"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox Grid.Row="0"
                 x:Name="ItemsList"
                 Background="#2C2C2E"
                 Foreground="#F2F2F7"
                 BorderBrush="#3A3A3C"
                 BorderThickness="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Text}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <TextBlock Grid.Row="1"
                   Text="选中后可重命名 / 上下移动 / 删除："
                   Foreground="#8E8E93"
                   FontSize="11"
                   Margin="0,8,0,4"/>

        <TextBox Grid.Row="2"
                 x:Name="EditTextBox"
                 Text="{Binding ElementName=ItemsList, Path=SelectedItem.Text, Mode=OneWay}"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="重命名" Click="OnRename" Margin="0,0,8,0"/>
            <Button Content="上移" Click="OnMoveUp" Margin="0,0,4,0"/>
            <Button Content="下移" Click="OnMoveDown" Margin="0,0,8,0"/>
            <Button Content="删除" Click="OnRemove" Foreground="#FF453A"/>
        </StackPanel>

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="NewItemText" KeyDown="OnNewItemKeyDown"/>
            <Button Grid.Column="1" Content="+ 添加" Click="OnAdd" Margin="8,0,0,0"/>
        </Grid>
    </Grid>
</Window>
