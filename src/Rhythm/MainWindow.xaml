<Window
    x:Class="Rhythm.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Rhythm"
    Title="Rhythm"
    Width="320"
    MinHeight="80"
    KeyDown="OnWindowKeyDown"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    SizeToContent="Height"
    SnapsToDevicePixels="True"
    UseLayoutRounding="True"
    WindowStartupLocation="Manual"
    WindowStyle="None">

    <Border
        x:Name="RootBorder"
        Padding="14"
        Background="{StaticResource WindowBackgroundBrush}"
        BorderBrush="{StaticResource DividerBrush}"
        BorderThickness="1"
        CornerRadius="10"
        Focusable="True"
        MouseLeftButtonDown="OnBorderMouseLeftButtonDown"
        SnapsToDevicePixels="True">
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Click="OnEditClick" Header="编辑事项..." />
                <MenuItem Click="OnHideClick" Header="隐藏窗口" />
                <Separator />
                <MenuItem Click="OnExitClick" Header="退出 Rhythm" />
            </ContextMenu>
        </Border.ContextMenu>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  Header Section  -->
            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Style="{StaticResource HeaderDateStyle}" Text="{Binding CurrentDate}" />
                <TextBlock Style="{StaticResource HeaderStatsStyle}" Text="{Binding ProgressText}" />
            </StackPanel>

            <!--  Pomodoro Section  -->
            <Border Grid.Row="1" Style="{StaticResource PomodoroCardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Style="{StaticResource PomodoroPhaseBadgeStyle}">
                            <TextBlock Style="{StaticResource PomodoroPhaseTextStyle}" Text="{Binding Pomodoro.PhaseLabel}" />
                        </Border>

                        <Button
                            Grid.Column="1"
                            Margin="10,0,0,0"
                            Click="OnPomodoroSettingsClick"
                            Content="设置"
                            Style="{StaticResource PomodoroSecondaryButtonStyle}" />
                    </Grid>

                    <TextBlock
                        Grid.Row="1"
                        Style="{StaticResource PomodoroTimerStyle}"
                        Text="{Binding Pomodoro.RemainingText}" />

                    <StackPanel Grid.Row="2" Margin="0,8,0,0">
                        <TextBlock Style="{StaticResource PomodoroMetaStyle}" Text="{Binding Pomodoro.CycleText}" />
                        <TextBlock Style="{StaticResource PomodoroMetaStyle}" Text="{Binding Pomodoro.LinkedItemText}" />
                    </StackPanel>

                    <StackPanel Grid.Row="3" Margin="0,12,0,0">
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <ComboBox
                                Grid.Column="0"
                                Margin="0,0,8,0"
                                DisplayMemberPath="Text"
                                IsEnabled="{Binding Pomodoro.IsIdle}"
                                ItemsSource="{Binding Pomodoro.AvailableItems}"
                                SelectedValue="{Binding Pomodoro.SelectedLinkedItemId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                SelectedValuePath="Id"
                                Style="{StaticResource PomodoroComboBoxStyle}" />

                            <Button
                                Grid.Column="1"
                                Click="OnPomodoroClearLinkedItemClick"
                                Content="清空"
                                IsEnabled="{Binding Pomodoro.IsIdle}"
                                Style="{StaticResource PomodoroCompactButtonStyle}" />
                        </Grid>

                        <UniformGrid Columns="4" Rows="1">
                            <Button
                                Margin="0,0,8,0"
                                Click="OnPomodoroStartOrResumeClick"
                                Content="开始/继续"
                                IsEnabled="{Binding Pomodoro.CanStartOrResume}"
                                Style="{StaticResource PomodoroPrimaryButtonStyle}" />
                            <Button
                                Margin="0,0,8,0"
                                Click="OnPomodoroPauseClick"
                                Content="暂停"
                                IsEnabled="{Binding Pomodoro.CanPause}"
                                Style="{StaticResource PomodoroSecondaryButtonStyle}" />
                            <Button
                                Margin="0,0,8,0"
                                Click="OnPomodoroSkipClick"
                                Content="跳过"
                                IsEnabled="{Binding Pomodoro.CanSkip}"
                                Style="{StaticResource PomodoroSecondaryButtonStyle}" />
                            <Button
                                Click="OnPomodoroResetClick"
                                Content="重置"
                                IsEnabled="{Binding Pomodoro.CanReset}"
                                Style="{StaticResource PomodoroSecondaryButtonStyle}" />
                        </UniformGrid>
                    </StackPanel>
                </Grid>
            </Border>

            <!--  Items Section with ScrollViewer  -->
            <ScrollViewer
                Grid.Row="2"
                MaxHeight="600"
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Auto">
                <Grid>
                    <ItemsControl ItemsSource="{Binding Items}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource ItemCardStyle}">
                                    <CheckBox IsChecked="{Binding IsCompleted, Mode=TwoWay}" Style="{StaticResource ItemCheckBoxStyle}">
                                        <CheckBox.Content>
                                            <TextBlock Style="{StaticResource ItemTextStyle}" Text="{Binding Text}" />
                                        </CheckBox.Content>
                                    </CheckBox>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock
                        Style="{StaticResource EmptyHintStyle}"
                        Text="今天没有事项"
                        Visibility="{Binding HasItems, Converter={StaticResource InverseBoolToVisibilityConverter}}" />
                </Grid>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
